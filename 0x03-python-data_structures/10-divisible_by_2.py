#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    new_list = []
    for a in new_list:
        if a % 2 == 0:
            return new_list.append(True)
        else:
            return new_list.append(False)
    return new_list